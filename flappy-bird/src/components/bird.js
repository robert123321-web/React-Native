import React from React;

const Bird = (props) => {
    return (
        <View
         style={{
            position: 'absolute',
            width: 50,
            height: 50,
            backgroundColor: 'yellow',
            left :100,
            bottom:birfBottom}}>
        </View>
    );

}

export default Bird;